# Test Generator

`generate_tests.py` creates the required deterministic Assignment 2 graph files and preserves the required filenames and vertex counts.

Run from the repository root:

```bash
python3 tools/generate_tests.py
```
