#ifndef TRIANGLE_H
#define TRIANGLE_H

#include <vector>

using namespace std;

int countTriangles(
    const vector<int>& offset,
    const vector<int>& edges,
    vector<vector<int>>& triangles
);

#endif
